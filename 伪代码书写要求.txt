文件名称
编写人
对应的函数行为的名称
使用的外部库文件（每个库文件写出所使用的函数名称）


被哪个内部函数调用，传进来的参数的类型与名称
调用的内部函数名称与类型，传递的参数名称及类型

读取的全局变量
读取并修改的全局变量
返回的值的类型与意义


具体的函数逻辑（涉及到不常用的外部库文件函数的时候，在伪代码中要标识。）

一个文件只含有一个函数行为或类似行为的几个函数。

内存的申请以及释放不可省略。
打开文件，申请内存空间后指针需进行判空。
关闭文件，释放内存，指针需要置空。

逻辑上出现问题及时群内反应。